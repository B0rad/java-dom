const myName = "Bort";

if (myName === "Bort") {
  console.log("hello");
}

const isLoggedIn = true;

if (isLoggedIn) {
  console.log("User is logged in");
}

if (!isLoggedIn) {
    console.log("user isn't logged in")
}

const enteredUserName = "Bart"

if (enteredUserName.length > 0) {
    console.log("Input is valid")
}